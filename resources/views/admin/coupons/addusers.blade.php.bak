<div class="row p-3" style="max-height:500px; overflow:auto;border-top: 1px solid #dee2e6;">
    <div class="col-md-12">
    
        @foreach($users as $user)
            <div class="media mb-3">
                <div class="n-chk">
                    <label class="new-control new-checkbox checkbox-success" style="height: 18px;">
                      <input type="checkbox" class="new-control-input" data-id="{{ $user->id }}"
                        @if($user->coupons->contains($coupon))
                            checked
                        @endif
                      >
                      <span class="new-control-indicator"></span>
                    </label>
                </div>
            
                <div class="media-body">
                    <h6 class="media-heading"><span class="media-title">{{ $user->firstname }} {{ $user->lastname }}</span></h6>
                    <p class="media-text">{{ $user->email }} | {{ $user->phone}}</p>
                </div>
            </div>
        @endforeach
    </div>
    
</div>